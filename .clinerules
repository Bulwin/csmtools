# CPL MANAGER Конвертер тактик

## Правила проекта

### Форматы данных
- UI-формат: JSON с объектами, содержащими координаты, углы обзора и другие параметры
- Массивный формат: Массивы массивов с точками для каждого игрока

### Особенности обработки
- syncTime всегда сохраняется как строка
- nade игнорируется, если все значения нулевые
- Поддержка до 5 игроков

### Метаданные тактики
- ID команды (числовое значение)
- Название тактики (строка)
- Карта (train, castle, mill)
- Сторона (CT=1, T=0)

### Структура файлов
- index.html - HTML-страница с пользовательским интерфейсом
- app.js - Обработка пользовательского ввода и взаимодействие с UI
- Converter.js - Функции конвертации между форматами
- stats.js - Загрузка и отображение статистики тактик из API

### API для статистики тактик
- URL: https://www.cplmanager.com/cpl/api/teams/{teamId}/tactics/stats?side={side}&map={map}
- side=red для T, side=blue для CT
- Формат ответа: массив объектов с информацией о тактиках
- Отображение: таблица с названием, ID, количеством раундов, побед и винрейтом
- Кнопка Play генерирует ссылку: https://www.cplmanager.com/cpl/team/tactics/simulation?tactic_id={tacticId}
